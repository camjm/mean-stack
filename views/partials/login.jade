div.col-sm-6.col-sm-offset-3

  h1
    span.fa.fa-lock
    |  Login

  // show any messages that come back with the authentication
  div(ng-show="message").alert.alert-danger {{ message }}

  form(name="loginForm" ng-submit="login(formData, loginForm)" novalidate)
    div(ng-class="{ 'has-error': showWarning(loginForm.email) }").form-group
      div.input-group
        span.input-group-addon
          i.fa.fa-envelope-o.fa-fw
        input(type="text" name="email" placeholder="email" ng-model="formData.email" required).form-control
      span(ng-show="showWarning(loginForm.email)").help-block Email is required
    div(ng-class="{ 'has-error': showWarning(loginForm.password) }").form-group
      div.input-group
        span.input-group-addon
          i.fa.fa-key.fa-fw
        input(type="password" name="password" placeholder="password" ng-model="formData.password" required).form-control
      span(ng-show="showWarning(loginForm.password)").help-block Password is required
    button(type="submit" ng-disabled="disableSubmit(loginForm)").btn.btn-warning Login

  hr

  p
    | Need an account?
    a(href="/signup")  Signup
  p
    | Or go
    a(href="/")  home
    |.

  p Login or Register with:

  a(href="/auth/google").btn.btn-danger
    span.fa.fa-google-plus
    |  Google
